git add -A
git commit -m "updated about"
git push


python -m http.server 8000


#Create and push your first revision tag
git tag -a rev1 -m "Rev 1 released"
git push origin main --tags

#When ready for the next release
git add -A
git commit -m "Prepare for Rev 2.4 release"
git tag -a rev2.4 -m "Rev 2.4 released"
git push origin main --tags


git add .
git commit -m "Updated Docs"

git push origin main
git push matrix main

vcbvcb